ADR-008

Decision

When a document is uploaded:

* Create a Document.
* Set the Document.title automatically from the uploaded filename without the extension.
* Preserve the original filename in DocumentAsset.original_filename.
* Allow the title to be edited later (V2 or later).