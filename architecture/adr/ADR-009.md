ADR-009

Decision

Database operations will execute inside a single transaction.

Operations outside the database (such as filesystem storage) will use compensating actions, not transactional rollback.
