ADR 24 — Modern SQLAlchemy Query API

Decision

Repositories will use SQLAlchemy 2.0’s select() API rather than the legacy session.query() interface.

Rationale

* Official SQLAlchemy 2.x style.
* Better type hints.
* Consistent with async SQLAlchemy.
* Aligns ORM and Core query construction.
* Future-proof.