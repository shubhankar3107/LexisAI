ADR 32 — Framework-Independent File Input

Decision: The service layer will not depend directly on FastAPI’s UploadFile. File content will be supplied through a framework-independent file/stream abstraction.

Rationale:

* Keeps business logic independent of FastAPI.
* Makes testing easier.
* Allows local filesystem and object-storage implementations later.
* Prevents framework-specific types from leaking into the service layer.