ADR 25 — Business Attributes Are Not Identifiers

Decision

Document.title is not unique.

Repositories should not expose APIs that imply uniqueness where none exists.

Consequences

Instead of:

get_by_title() -> Document | None

we’ll implement:

list_by_title() -> list[Document]

This keeps the repository aligned with the actual domain.
